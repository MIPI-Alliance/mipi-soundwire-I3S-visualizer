# SWI3S Studio dependencies.
#
# Install into ONE Python interpreter — the app needs the data stack AND the Qt
# GUI stack in the same interpreter:
#
#     python3 -m pip install -r requirements.txt
#
# Then build the native decode core and run the app (see README ▸ Running):
#
#     python3 -m pip install ./native      # builds the swi3score C++ module
#     PYTHONPATH=. python3 -m swi3s_studio.app

# Runtime — data + model
numpy>=1.24
pyarrow>=14.0

# Runtime — memory headroom for the large-capture open guard (ingest/saleae_sal.py).
# Optional in the sense that the guard falls back to a conservative fixed budget
# without it, but then it cannot see ACTUAL free memory — declare it so the accurate
# path is the normal one.
psutil>=5.9

# Runtime — Qt GUI
PySide6>=6.6
pyqtgraph>=0.13

# Build — native swi3score decode core (pybind11); required by `pip install ./native`
pybind11>=2.11
