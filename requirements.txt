# SWI3S Visualizer Requirements
#
# Required Python version: 3.8+
#
# IMPORTANT: Tk 9.0+ is required on macOS for button events to work properly.
# - Python 3.13+ includes Tk 9.0 by default
# - Python 3.11/3.12 on macOS ships with Tk 8.6 which has event handling bugs
# - Check your Tk version: python3 -c "import tkinter; print(tkinter.TkVersion)"
#
# On macOS with older Python, install Python 3.13+ from python.org or use:
#   brew install python-tk@3.13
#
# Install packages with the same Python you'll use to run the app:
#   python3 -m pip install -r requirements.txt
#   python3 swi3s_visualizer.py

customtkinter>=5.0.0
canvasvg>=1.0.0

# Note: tkinter is also required (standard library)
# - Included with Python on macOS/Windows
# - On Linux: sudo apt-get install python3-tk
